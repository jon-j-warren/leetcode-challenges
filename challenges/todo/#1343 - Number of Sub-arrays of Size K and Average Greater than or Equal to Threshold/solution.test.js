const { _PLACEHOLDER_ } = require('./solution');

describe("Number of Sub-arrays of Size K and Average Greater than or Equal to Threshold", () => {
    // Example 1
    test("it should pass Example 1", () => {
        const input = [
            
        ];
        const output = '';
        expect(_PLACEHOLDER_(...input)).toEqual(output);
    });

    // Example 2
    test("it should pass Example 2", () => {
        const input = [
            
        ];
        const output = '';
        expect(_PLACEHOLDER_(...input)).toEqual(output);
    });

    // Example 3
    test("it should pass Example 3", () => {
        const input = [
            
        ];
        const output = '';
        expect(_PLACEHOLDER_(...input)).toEqual(output);
    });
});
            