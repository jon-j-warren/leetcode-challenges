{
  "name": "generate_boilerplate",
  "version": "1.0.0",
  "description": "Generates the boilerplate for the challenge solutions",
  "main": "fetch_questions.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fetch_questions": "node --experimental-json-modules fetch_questions.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "node-fetch": "^2.6.1"
  }
}
